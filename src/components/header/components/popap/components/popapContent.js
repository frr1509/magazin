import styled from "styled-components";

const PopapContentContainer = ({ className }) => {
    return <div className={className}>Popap Content</div>;
};

export const PopapContent = styled(PopapContentContainer)``;
