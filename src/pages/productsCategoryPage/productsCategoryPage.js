import styled from "styled-components";

const ProductsCategoryPageContainer = ({ className }) => {
    return <div className={className}></div>;
};

export const ProductsCategoryPage = styled(ProductsCategoryPageContainer)``;
